---
title: PCB
description: Generated reference for PCB board, footprint, pad, and placement helpers.
---
This page is generated from the public Python source. Add or improve docstrings in `python/volt` to improve the reference.

## `volt.Board`

Python handle to one kernel-owned PCB projection.

Source: `python/volt/pcb.py`

### Properties

| Property | Summary |
| --- | --- |
| `center` | Return the center anchor of the board outline bounding box. |

### Methods

| Method | Summary |
| --- | --- |
| `design_rules(self) -> dict[str, float]` | Return the board design-rule values currently stored in the kernel. |
| `set_design_rules(self, *, copper_clearance: float \| None = None, min_track_width: float \| None = None, min_via_drill: float \| None = None, min_via_annular: float \| None = None, board_outline_clearance: float \| None = None) -> Board` | Update board design rules, preserving unspecified rule values. |
| `add_layer(self, name: str, *, role: str, side: str, thickness: float = 0.0, enabled: bool = True) -> int` | Add a physical or logical board layer and return its kernel index. |
| `set_layer_stack(self, layers: Iterable[int], *, thickness: float) -> Board` | Set the board layer stack order and total thickness. |
| `set_rectangular_outline(self, *, origin: Point, size: Point) -> Board` | Set a rectangular board outline from an origin and size. |
| `set_polygon_outline(self, vertices: Iterable[Point]) -> Board` | Set a polygon board outline from ordered vertices. |
| `edge(self, name: str)` | Return a mechanical board edge helper. |
| `corner(self, name: str)` | Return a mechanical board corner anchor. |
| `layout(self, *, at: Point = (0, 0), direction: str = 'Right', unit: float = 1.0, grid: float \| None = None)` | Create a schematic-style PCB placement authoring session. |
| `add(self, primitive) -> int` | Add a generic board primitive and return its kernel index. |
| `cache_footprint(self, footprint: Footprint) -> int` | Cache an explicit board-owned footprint definition for importers and low-level tests. |
| `place(self, component: Component \| int, *, at: Point, rotation: float = 0.0, side: str = 'top', locked: bool = False) -> int` | Place a component footprint on the board and return the placement index. |
| `add_track(self, net: Net \| int, *, layer: int, points: Iterable[Point], width: float) -> int` | Add a routed track segment sequence for a logical net. |
| `add_via(self, net: Net \| int, *, at: Point, start_layer: int, end_layer: int, drill: float = 0.3, annular: float = 0.7) -> int` | Add a via connecting a net between two board layers. |
| `add_zone(self, *, outline: Iterable[Point], layers: Iterable[int], net: Net \| int \| None = None, fill: str = 'solid', priority: int = 0) -> int` | Add a copper zone, optionally bound to a logical net. |
| `add_keepout(self, *, outline: Iterable[Point], layers: Iterable[int], restrictions: Iterable[str]) -> int` | Add a keepout region with layer and feature restrictions. |
| `add_text(self, text: str, *, at: Point, layer: int, rotation: float = 0.0, size: float = 1.0, locked: bool = False) -> int` | Add a board text item and return its kernel index. |
| `resolve_pads(self) -> tuple[PadResolution, ...]` | Resolve placed footprint pads to component pins and logical nets. |
| `validate(self) -> DiagnosticReport` | Run PCB projection validation and return the diagnostic report. |
| `to_json(self) -> str` | Serialize the PCB projection to Volt board JSON. |
| `to_svg(self, *, pad_net_overlays: bool = True, diagnostic_overlays: bool = True, ratsnest_edges: bool = True) -> str` | Render the PCB projection as SVG. |
| `to_kicad_pcb(self) -> KiCadPcbExport` | Export the PCB projection to a KiCad `.kicad_pcb` adapter document. |
| `write_json(self, path: str \| Path) -> None` | Write the PCB projection JSON to a file. |
| `write_svg(self, path: str \| Path) -> None` | Write the PCB projection SVG to a file. |
| `write_kicad_pcb(self, path: str \| Path) -> KiCadPcbExport` | Write the KiCad PCB adapter document and return its loss report. |

## `volt.BoardAnchor`

Board point that can be offset directionally while preserving author intent.

Source: `python/volt/_pcb_layout.py`

### Properties

| Property | Summary |
| --- | --- |
| `x: float` | Return the board x coordinate. |
| `y: float` | Return the board y coordinate. |
| `point: tuple[float, float]` | Return this anchor as an ``(x, y)`` tuple. |

### Methods

| Method | Summary |
| --- | --- |
| `offset(self, dx: float = 0, dy: float = 0) -> BoardAnchor` | Return a new anchor offset from this point. |
| `left(self, distance: float) -> BoardAnchor` | Return a new anchor moved left by a distance. |
| `right(self, distance: float) -> BoardAnchor` | Return a new anchor moved right by a distance. |
| `up(self, distance: float) -> BoardAnchor` | Return a new anchor moved up by a distance. |
| `down(self, distance: float) -> BoardAnchor` | Return a new anchor moved down by a distance. |
| `tox(self, anchor_or_x) -> BoardAnchor` | Return an anchor at this y coordinate and the target x coordinate. |
| `toy(self, anchor_or_y) -> BoardAnchor` | Return an anchor at this x coordinate and the target y coordinate. |
| `__iter__(self)` | Iterate over the ``(x, y)`` coordinate values. |

## `volt.BoardLayout`

Cursor state for schematic-style PCB placement authoring.

Source: `python/volt/_pcb_layout.py`

### Properties

| Property | Summary |
| --- | --- |
| `here: BoardAnchor` | Return the layout cursor's current anchor. |
| `direction: str` | Return the layout cursor's current direction. |
| `unit: float` | Return the layout unit used by stack spacing defaults. |
| `grid: float \| None` | Return the placement grid spacing, if explicit coordinates are snapped. |

### Methods

| Method | Summary |
| --- | --- |
| `snap(self, point: tuple[float, float] \| BoardAnchor \| None = None) -> BoardAnchor` | Return an anchor snapped to the layout grid. |
| `snap_x(self, anchor_or_x) -> float` | Return an x coordinate snapped to the layout grid. |
| `snap_y(self, anchor_or_y) -> float` | Return a y coordinate snapped to the layout grid. |
| `move(self, *, dx: float = 0, dy: float = 0) -> BoardLayout` | Move the layout cursor by a relative offset. |
| `move_from(self, anchor: tuple[float, float] \| BoardAnchor, *, dx: float = 0, dy: float = 0, direction: str \| None = None) -> BoardLayout` | Move the layout cursor to an anchor plus an optional relative offset. |
| `place(self, component: Component, *, at: tuple[float, float] \| BoardAnchor \| None = None, orient: str \| None = None, side: str = 'top', locked: bool = False) -> PlacedBoardComponent` | Place a component at the cursor or an explicit board anchor. |
| `two_pad(self, component: Component, *, side: str = 'top', locked: bool = False) -> BoardTwoPadComponent` | Start fluent placement for a two-pad component footprint. |
| `route(self, net: Net \| int, *, layer: int, width: float = 0.2, mode: str = 'octilinear') -> BoardRoute` | Start a schematic-style routed track sequence from the cursor. |
| `via(self, net: Net \| int, *, at: tuple[float, float] \| BoardAnchor \| None = None, start_layer: int, end_layer: int, drill: float = 0.3, annular: float = 0.7) -> int` | Add a via at a board anchor and move the cursor to that anchor. |
| `node(self, at: tuple[float, float] \| BoardAnchor \| None = None, *, dx: float = 0, dy: float = 0) -> BoardAnchor` | Return a reusable board-local geometry anchor without adding objects. |
| `stack(self, *, count: int, direction: str \| None = None, pitch: float \| None = None, at: tuple[float, float] \| BoardAnchor \| None = None) -> tuple[BoardAnchor, ...]` | Return evenly spaced anchors from the current cursor or an explicit anchor. |
| `hold(self)` | Temporarily author from the current cursor, then restore cursor state. |
| `frame(self, at: tuple[float, float] \| BoardAnchor = (0, 0), *, direction: str \| None = None)` | Temporarily author in a local board coordinate frame. |

## `volt.BoardPadAnchor`

Anchor for one resolved footprint pad on a placed component.

Source: `python/volt/_pcb_layout.py`

## `volt.BoardRoute`

Fluent builder for hand-placed PCB route geometry.

Source: `python/volt/_pcb_layout.py`

### Methods

| Method | Summary |
| --- | --- |
| `at(self, point: tuple[float, float] \| BoardAnchor) -> BoardRoute` | Start this route at an explicit board anchor. |
| `to(self, point: tuple[float, float] \| BoardAnchor, *, mode: str \| None = None) -> int` | Terminate this route at an anchor and add the track to the board. |
| `tox(self, anchor_or_x) -> BoardRoute` | Route horizontally to the target x coordinate. |
| `toy(self, anchor_or_y) -> BoardRoute` | Route vertically to the target y coordinate. |
| `left(self, distance: float) -> BoardRoute` | Route left by a relative distance. |
| `right(self, distance: float) -> BoardRoute` | Route right by a relative distance. |
| `up(self, distance: float) -> BoardRoute` | Route up by a relative distance. |
| `down(self, distance: float) -> BoardRoute` | Route down by a relative distance. |

## `volt.BoardTwoPadComponent`

Fluent builder for one two-pad PCB component placement.

Source: `python/volt/_pcb_layout.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel placement index after materialization. |
| `component: Component` | Return the component being placed. |
| `center: BoardAnchor` | Return the placed footprint center after materialization. |
| `start: BoardPadAnchor` | Return the start pad anchor after materialization. |
| `end: BoardPadAnchor` | Return the end pad anchor after materialization. |

### Methods

| Method | Summary |
| --- | --- |
| `at(self, point: tuple[float, float] \| BoardAnchor) -> BoardTwoPadComponent` | Set the anchor point for the two-pad placement. |
| `anchor(self, ref: str \| int) -> BoardTwoPadComponent` | Choose which local pad or pin anchor is fixed to ``at``. |
| `drop(self, ref: str \| int) -> BoardTwoPadComponent` | Choose which local pad or pin anchor drives the cursor endpoint. |
| `right(self) -> PlacedBoardComponent` | Orient the footprint so its end pad is to the right of its start pad. |
| `left(self) -> PlacedBoardComponent` | Orient the footprint so its end pad is to the left of its start pad. |
| `up(self) -> PlacedBoardComponent` | Orient the footprint so its end pad is above its start pad. |
| `down(self) -> PlacedBoardComponent` | Orient the footprint so its end pad is below its start pad. |
| `pad(self, label: str) -> BoardPadAnchor` | Materialize and return a pad anchor by footprint pad label. |
| `pin(self, key: int \| str) -> BoardPadAnchor` | Materialize and return a pin anchor by logical pin number or name. |
| `__getitem__(self, key: int \| str) -> BoardPadAnchor` | Materialize and return a pin anchor by logical pin number or name. |

## `volt.Circle`

Generic circular board-side primitive.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `center: Point` |
| `diameter: float` |
| `label: str` |
| `side: str` |
| `role: str` |

## `volt.Cutout`

Generic board cutout primitive.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `outline: tuple[Point, ...]` |
| `role: str` |
| `label: str` |

### Methods

| Method | Summary |
| --- | --- |
| `polygon(cls, vertices: Iterable[Point], *, role: str = '', label: str = '') -> 'Cutout'` | Create a polygonal board cutout. |

## `volt.Footprint`

Library-qualified footprint geometry for board-ready physical parts.

Source: `python/volt/_footprint.py`

### Fields

| Field |
| --- |
| `ref: FootprintRef` |
| `pads: tuple[Any, ...]` |

### Properties

| Property | Summary |
| --- | --- |
| `library: str` | Return the footprint library name from the footprint reference. |
| `name: str` | Return the footprint name from the footprint reference. |

## `volt.FootprintDefinition`

Public value or alias exported by `volt`.

Source: `python/volt/pcb.py`

```python
FootprintDefinition = Footprint
```

## `volt.FootprintDrill`

Drill metadata for a through-hole footprint pad.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `diameter: float` |
| `plating: str` |

## `volt.FootprintPad`

Normalized footprint pad geometry for board-ready footprints.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `label: str` |
| `kind: str` |
| `shape: str` |
| `position: Point` |
| `size: Point` |
| `layers: str` |
| `drill: FootprintDrill \| None` |
| `mechanical_role: str \| None` |

### Methods

| Method | Summary |
| --- | --- |
| `surface_mount(cls, label: str, *, at: Point, size: Point, shape: str = 'rounded_rectangle', layers: str = 'front_smd', mechanical_role: str \| None = None) -> FootprintPad` | Create a surface-mount footprint pad definition. |
| `through_hole(cls, label: str, *, at: Point, size: Point, drill: FootprintDrill, shape: str = 'circle', layers: str = 'through_hole', mechanical_role: str \| None = None) -> FootprintPad` | Create a through-hole footprint pad definition with drill metadata. |

## `volt.Hole`

Generic circular board hole primitive.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `center: Point` |
| `diameter: float` |
| `plated: bool` |
| `role: str` |
| `label: str` |
| `finished_diameter: float \| None` |

## `volt.KiCadLossWarning`

Structured warning for an unsupported or lossy KiCad adapter construct.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `kind: str` |
| `construct: str` |
| `message: str` |
| `severity: str` |

## `volt.KiCadPcbExport`

Result of exporting a board projection to a KiCad PCB file.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `text: str` |
| `warnings: tuple[KiCadLossWarning, ...]` |

## `volt.MechanicalKeepout`

Generic mechanical keepout primitive.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `outline: tuple[Point, ...]` |
| `layers: tuple[int, ...]` |
| `restrictions: tuple[str, ...]` |

## `volt.PlacedBoardComponent`

Read-only handle to one placed PCB component footprint.

Source: `python/volt/_pcb_layout.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel placement index. |
| `component: Component` | Return the logical component represented by this placement. |
| `rotation: float` | Return the resolved board rotation in degrees. |
| `side: str` | Return the board side for this placement. |
| `center: BoardAnchor` | Return the placement center anchor. |
| `start: BoardPadAnchor` | Return the first logical pin's pad anchor for a two-pin component. |
| `end: BoardPadAnchor` | Return the last logical pin's pad anchor for a two-pin component. |

### Methods

| Method | Summary |
| --- | --- |
| `pad(self, label: str) -> BoardPadAnchor` | Return a resolved pad anchor by footprint pad label. |
| `pin(self, key: int \| str) -> BoardPadAnchor` | Return a resolved pad anchor by logical pin number or name. |
| `pins(self, name: str) -> tuple[BoardPadAnchor, ...]` | Return every resolved pad anchor for logical pins with the given name. |
| `pin_anchors(self) -> tuple[BoardPadAnchor, ...]` | Return resolved pad anchors for every logical pin with a single mapped pad. |
| `__getitem__(self, key: int \| str) -> BoardPadAnchor` | Return a placed component pin anchor by pin name or number. |

## `volt.PadResolution`

Derived board-space resolution for one placed footprint pad.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `placement: int` |
| `component: int` |
| `pad: int` |
| `pad_label: str` |
| `position: Point` |
| `pin: int \| None` |
| `net: int \| None` |
| `status: str` |

## `volt.Slot`

Generic slotted board hole primitive.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `start: Point` |
| `end: Point` |
| `width: float` |
| `plated: bool` |
| `role: str` |
| `label: str` |

## `volt.Text`

Generic board text/annotation primitive.

Source: `python/volt/pcb.py`

### Fields

| Field |
| --- |
| `text: str` |
| `at: Point` |
| `layer: int` |
| `rotation: float` |
| `size: float` |
| `locked: bool` |
