---
title: Project
description: Generated reference for staged project workflows, results, diagnostics, and tests.
---
This page is generated from the public Python source. Add or improve docstrings in `python/volt` to improve the reference.

## `volt.BuildContext`

Read-only access to models produced by earlier project stages.

Source: `python/volt/project.py`

### Properties

| Property | Summary |
| --- | --- |
| `designs: tuple[Design, ...]` | Return designs produced by the project design stage. |
| `resources: tuple[ProjectResource, ...]` | Return explicit authoring resources produced by prior stages. |

### Methods

| Method | Summary |
| --- | --- |
| `design(self, name: str \| None = None) -> Design` | Return a design by stable name, or the only design for the common case. |
| `resource(self, name: str, expected_type: type \| tuple[type, ...] \| None = None) -> object` | Return a named authoring resource from an earlier stage. |

## `volt.ExpectedDiagnostic`

Project-local policy for a known diagnostic.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `code: str` |
| `severity: str \| None` |
| `stage: str \| None` |
| `source: str \| None` |
| `report: str \| None` |
| `design: str \| None` |
| `board: str \| None` |
| `rule: str \| None` |

### Properties

| Property | Summary |
| --- | --- |
| `expect_diagnostic_kwargs: dict[str, str]` | Return this policy as ``Project.expect_diagnostic`` keyword arguments. |

### Methods

| Method | Summary |
| --- | --- |
| `matches(self, diagnostic: ProjectDiagnostic) -> bool` | Return whether this expectation covers a project diagnostic. |

## `volt.ExpectedDiagnosticResult`

Match result for one expected diagnostic policy item.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `code: str` |
| `severity: str \| None` |
| `stage: str \| None` |
| `source: str \| None` |
| `report: str \| None` |
| `design: str \| None` |
| `board: str \| None` |
| `rule: str \| None` |
| `matched: bool` |

### Properties

| Property | Summary |
| --- | --- |
| `expect_diagnostic_kwargs: dict[str, str]` | Return the checked policy as ``Project.expect_diagnostic`` keyword arguments. |

## `volt.ManufacturingPackageError`

Raised when a project result cannot be exported as a manufacturing package.

Source: `python/volt/manufacturing.py`

## `volt.ManufacturingPackageResult`

Paths and selected metadata for a written manufacturing package.

Source: `python/volt/manufacturing.py`

### Fields

| Field |
| --- |
| `output: Path` |
| `archive: Path \| None` |
| `board: dict[str, str]` |
| `native_fabrication: dict[str, object]` |

## `volt.Project`

Canonical staged entrypoint for a Volt electronics project.

Source: `python/volt/project.py`

### Methods

| Method | Summary |
| --- | --- |
| `use_library(self, library: Library) -> Library` | Include a library's validation diagnostics in this project result. |
| `expect_diagnostic(self, diagnostic: ProjectDiagnostic \| ExpectedDiagnostic \| ExpectedDiagnosticResult \| None = None, *, code: str \| None = None, severity: str \| None = None, stage: ProjectStage \| str \| None = None, source: str \| None = None, report: str \| None = None, design: str \| None = None, board: str \| None = None, rule: str \| None = None) -> ExpectedDiagnostic` | Declare a project-local diagnostic that is expected for this run. |
| `run(self) -> 'ProjectResult'` | Run the registered project stages and return one result. |
| `run_through(self, stage: ProjectStage) -> 'ProjectResult'` | Run stages through the given stage handle. |

## `volt.ProjectArtifactPaths`

Flat artifact paths written for example and viewer consumers.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `logical_json: Path \| None` |
| `schematic_json: Path \| None` |
| `schematic_svg: Path \| None` |
| `schematic_body_svg: Path \| None` |
| `schematic_svg_pages: tuple[Path, ...]` |
| `pcb_json: Path \| None` |
| `pcb_svg: Path \| None` |
| `pcb_layer_svgs: tuple[Path, ...]` |
| `kicad_pcb: Path \| None` |
| `cpl_json: Path \| None` |
| `cpl_csv: Path \| None` |
| `diagnostics_json: Path \| None` |

## `volt.ProjectDiagnostic`

Diagnostic emitted during one project run with project source metadata.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `stage: str` |
| `source: str` |
| `report: str` |
| `severity: str` |
| `code: str` |
| `message: str` |
| `entities: tuple[DiagnosticEntity, ...]` |
| `category: str` |
| `overlays: tuple[DiagnosticOverlay, ...]` |
| `measurement: DiagnosticMeasurement \| None` |
| `design: str \| None` |
| `board: str \| None` |
| `rule: str \| None` |

### Properties

| Property | Summary |
| --- | --- |
| `expect_diagnostic_kwargs: dict[str, str]` | Return copyable ``Project.expect_diagnostic`` keyword arguments. |

## `volt.ProjectDiagnostics`

Ordered project diagnostics collected from model validators.

Source: `python/volt/project.py`

### Properties

| Property | Summary |
| --- | --- |
| `has_errors: bool` | Return whether any collected diagnostic has error severity. |

### Methods

| Method | Summary |
| --- | --- |
| `__iter__(self)` | Iterate over diagnostics in deterministic report order. |
| `__len__(self) -> int` | Return the number of diagnostics. |
| `errors(self, *, stage: ProjectStage \| str \| None = None) -> tuple[ProjectDiagnostic, ...]` | Return error diagnostics, optionally filtered by stage. |

## `volt.ProjectResource`

Explicit non-canonical authoring resource produced by a project stage.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `name: str` |
| `value: object` |

## `volt.ProjectResult`

Output of one Volt project run.

Source: `python/volt/project.py`

### Properties

| Property | Summary |
| --- | --- |
| `ok: bool` | Return whether the project run has no errors. |
| `clean: bool` | Return whether the project run has no diagnostics or test failures. |
| `status: str` | Return clean, expected-diagnostics, or failed project status. |
| `diagnostics: ProjectDiagnostics` | Return diagnostics collected from default project checks. |
| `expected_diagnostics: tuple[ExpectedDiagnosticResult, ...]` | Return declared expected diagnostic policy with match status. |
| `expected_project_diagnostics: tuple[ProjectDiagnostic, ...]` | Return collected diagnostics covered by expected diagnostic policy. |
| `unexpected_diagnostics: tuple[ProjectDiagnostic, ...]` | Return collected diagnostics not covered by expected diagnostic policy. |
| `missing_expected_diagnostics: tuple[ExpectedDiagnosticResult, ...]` | Return expected diagnostics that did not appear in the project run. |
| `expected_diagnostics_ok: bool` | Return whether all diagnostics are expected and all expectations matched. |
| `stages: tuple[StageResult, ...]` | Return executed stage summaries in run order. |
| `designs: tuple[Design, ...]` | Return logical designs collected by the run. |
| `schematics: tuple[Schematic, ...]` | Return schematic projections collected by the run. |
| `boards: tuple[Board, ...]` | Return board projections collected by the run. |

### Methods

| Method | Summary |
| --- | --- |
| `design(self, name: str \| None = None) -> Design` | Return a collected design by name, or the only design. |
| `schematic(self, name: str \| None = None) -> Schematic` | Return a collected schematic by name, or the only schematic. |
| `board(self, name: str \| None = None) -> Board` | Return a collected board by name, or the only board. |
| `stage(self, stage: ProjectStage \| str) -> StageResult` | Return an executed stage summary by stage handle or name. |
| `test_failures(self) -> tuple[ProjectTestResult, ...]` | Return failed project tests in stage execution order. |
| `write(self, path: str \| Path, *, profile: str = 'default') -> None` | Write a deterministic project result bundle, optionally with viewer-profile checks. |
| `write_manufacturing_package(self, path: str \| Path, *, board: str \| None = None, manufacturing_profile: Mapping[str, str] \| None = None, archive: bool = False) -> 'ManufacturingPackageResult'` | Write a deterministic native manufacturing package for this project result. |
| `write_artifacts(self, path: str \| Path, *, slug: str \| None = None, pcb_svg_options: dict[str, object] \| None = None) -> ProjectArtifactPaths` | Write standard flat example/viewer artifacts and return their paths. |

## `volt.ProjectStage`

Named workflow stage used as a decorator and result lookup key.

Source: `python/volt/project.py`

### Properties

| Property | Summary |
| --- | --- |
| `registered: bool` | Return whether this stage has a composition function. |
| `tests: tuple[ProjectTestDefinition, ...]` | Return product-intent tests registered for this stage. |

### Methods

| Method | Summary |
| --- | --- |
| `test(self, function: BuildFunction) -> BuildFunction` | Register a product-intent test for this stage. |

## `volt.ProjectTestDefinition`

Registered product-intent test for one project stage.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `name: str` |
| `function: BuildFunction` |

## `volt.ProjectTestResult`

Result of one product-intent project test.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `stage: str` |
| `name: str` |
| `ok: bool` |
| `message: str` |

## `volt.StageResult`

Summary for one executed project stage.

Source: `python/volt/project.py`

### Fields

| Field |
| --- |
| `name: str` |
| `model_count: int` |
| `tests: tuple['ProjectTestResult', ...]` |
