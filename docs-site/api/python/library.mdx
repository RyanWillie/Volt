---
title: Library
description: Generated reference for library, component, pin, and schematic symbol specs.
---
This page is generated from the public Python source. Add or improve docstrings in `python/volt` to improve the reference.

## `volt.Library`

Collection of reusable Python-authored component definitions.

Source: `python/volt/library.py`

### Properties

| Property | Summary |
| --- | --- |
| `parts: tuple[Part, ...]` | Return registered public parts in deterministic order. |

### Methods

| Method | Summary |
| --- | --- |
| `add(self, part: Part) -> Part` | Add a reusable public part to this library. |
| `build(self) -> LibraryResult` | Validate this library's public parts and return a deterministic result. |
| `part_family(self, **defaults) -> _PartFamily` | Create a helper that registers parts using shared defaults. |
| `component(self, name: str, *, pins: Iterable[PinSpec], properties: dict \| None = None, source_name: str \| None = None, source_version: str \| None = None, physical_part: PhysicalPartSpec \| None = None, prefix: str = 'U', schematic_symbol: SchematicSymbolSpec \| Iterable[SchematicSymbolSpec] \| None = None) -> LibraryComponent` | Register a reusable component entry in this Python library. |
| `__getitem__(self, name: str) -> Part \| LibraryComponent` | Return a registered public part or legacy library component by name. |

## `volt.LibraryComponent`

Reusable component entry owned by a Python library.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `library: Library` |
| `name: str` |
| `pins: tuple[PinSpec, ...]` |
| `properties: dict` |
| `source_name: str` |
| `source_version: str` |
| `physical_part: PhysicalPartSpec \| None` |
| `prefix: str` |
| `schematic_symbols: tuple[SchematicSymbolSpec, ...]` |

### Properties

| Property | Summary |
| --- | --- |
| `cache_key: tuple[str, str, str, str]` | Return the stable design-local cache key for this library component. |
| `schematic_symbol: SchematicSymbolSpec \| None` | Return this component's default schematic symbol, if one is registered. |

## `volt.LibraryDiagnostic`

Diagnostic emitted while validating one library build.

Source: `python/volt/library_result.py`

### Fields

| Field |
| --- |
| `source: str` |
| `report: str` |
| `severity: str` |
| `code: str` |
| `message: str` |
| `entities: tuple[object, ...]` |

## `volt.LibraryDiagnostics`

Ordered library diagnostics collected from part validation.

Source: `python/volt/library_result.py`

### Properties

| Property | Summary |
| --- | --- |
| `has_errors: bool` | Return whether any collected diagnostic has error severity. |

### Methods

| Method | Summary |
| --- | --- |
| `__iter__(self) -> Iterator[LibraryDiagnostic]` | Iterate over diagnostics in deterministic report order. |
| `__len__(self) -> int` | Return the number of diagnostics. |
| `__getitem__(self, index: int) -> LibraryDiagnostic` | Return one diagnostic by positional index. |
| `errors(self, *, source: str \| None = None) -> tuple[LibraryDiagnostic, ...]` | Return error diagnostics, optionally filtered by source. |

## `volt.LibraryPartResult`

Validation status for one public library part.

Source: `python/volt/library_result.py`

### Fields

| Field |
| --- |
| `name: str` |
| `schematic_ready: bool` |
| `board_ready: bool` |
| `serializable: bool` |
| `has_footprint: bool` |
| `pad_mapping_complete: bool` |
| `diagnostics: tuple[LibraryDiagnostic, ...]` |

## `volt.LibraryResult`

Output of one deterministic library validation run.

Source: `python/volt/library_result.py`

### Properties

| Property | Summary |
| --- | --- |
| `ok: bool` | Return whether the library build has no error diagnostics. |
| `diagnostics: LibraryDiagnostics` | Return diagnostics collected during library validation. |
| `parts: tuple[LibraryPartResult, ...]` | Return per-part validation summaries in deterministic name order. |

### Methods

| Method | Summary |
| --- | --- |
| `part(self, name: str) -> LibraryPartResult` | Return a per-part validation summary by part name. |
| `to_dict(self) -> dict` | Return a deterministic dictionary payload for this library result. |

## `volt.PinSpec`

Reusable pin definition data for Python-authored component definitions.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `name: str` |
| `number: int \| str` |
| `role: str \| None` |
| `requirement: str` |
| `terminal: str` |
| `direction: str` |
| `signal: str` |
| `drive: str` |
| `polarity: str` |
| `voltage_range: tuple[float \| None, float \| None] \| None` |

## `volt.PhysicalPartSpec`

Reusable selected physical part data; board-ready real parts should carry Footprint objects.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `manufacturer: str` |
| `part_number: str` |
| `package: str` |
| `footprint: FootprintInput` |
| `pin_pads: dict[int \| str, PinPadValue] \| None` |
| `properties: dict \| None` |
| `voltage_rating: float \| None` |
| `power_rating: float \| None` |
| `model_3d: PartModel3D \| None` |
| `same_numbered_pads: bool` |

### Methods

| Method | Summary |
| --- | --- |
| `same_numbered(cls, *, manufacturer: str, part_number: str, package: str, footprint: FootprintInput, properties: dict \| None = None, voltage_rating: float \| None = None, power_rating: float \| None = None, model_3d: PartModel3D \| None = None) -> PhysicalPartSpec` | Create a physical part whose footprint pad labels match pin numbers. |
| `pin_pads_for(self, component: LibraryComponent) -> dict[int \| str, PinPadValue]` | Return the pin-to-pad mapping for a library component. |

## `volt.Part`

Reusable public part definition for Python-authored Volt libraries.

Source: `python/volt/part.py`

### Properties

| Property | Summary |
| --- | --- |
| `library: Library \| None` | Return the library this part was added to, if any. |
| `schematic_symbol: SchematicSymbolSpec \| None` | Return this part's default schematic symbol, if one is registered. |
| `part_number: str \| None` | Return the manufacturer part number carried by this part. |

## `volt.PartModel3D`

Reusable 3D asset reference and footprint-relative transform for a selected part.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `source: str \| Path` |
| `offset: tuple[float, float, float]` |
| `rotation: float` |

### Properties

| Property | Summary |
| --- | --- |
| `source_path: Path` | Return the authoring-time source path for this model asset. |
| `file_name: str` | Return the source file name used for viewer bundle metadata. |
| `format: str` | Return the normalized model asset format. |

## `volt.SchematicBlockPinSpec`

Pin placement input for generic block and IC schematic symbols.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `name: str` |
| `number: int \| str` |
| `side: str` |
| `slot: int \| None` |
| `label: str \| None` |

## `volt.SchematicBlockSideLayoutSpec`

Per-side placement overrides for generic block and IC schematic symbols.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `side: str` |
| `pad: float \| None` |
| `lead_length: float \| None` |
| `pin_pitch: float \| None` |
| `pin_label_offset: float \| None` |
| `pin_number_offset: float \| None` |

## `volt.SchematicSymbolPinSpec`

Reusable visual pin anchor data for Python-authored schematic symbols.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `name: str` |
| `number: int \| str` |
| `at: tuple[float, float]` |
| `orientation: str` |

## `volt.SchematicSymbolSpec`

Reusable Volt-native schematic symbol data for Python-authored libraries.

Source: `python/volt/library.py`

### Fields

| Field |
| --- |
| `name: str` |
| `pins: tuple[SchematicSymbolPinSpec, ...]` |
| `primitives: tuple[dict, ...]` |
| `variant: str` |

### Methods

| Method | Summary |
| --- | --- |
| `pin(name: str, number: int \| str, at: tuple[float, float], orientation: str = 'Right') -> SchematicSymbolPinSpec` | Create a pin anchor for a custom schematic symbol. |
| `block_pin(name: str, number: int \| str, *, side: str, slot: int \| None = None, label: str \| None = None) -> SchematicBlockPinSpec` | Create a pin placement entry for block-style schematic symbols. |
| `ic_pin(name: str, number: int \| str, *, side: str, slot: int \| None = None, label: str \| None = None) -> SchematicBlockPinSpec` | Create a pin placement entry for IC-style schematic symbols. |
| `side_layout(side: str, *, pad: float \| None = None, lead_length: float \| None = None, pin_pitch: float \| None = None, pin_label_offset: float \| None = None, pin_number_offset: float \| None = None) -> SchematicBlockSideLayoutSpec` | Create per-side layout overrides for a block-style symbol. |
| `block(name: str, *, pins: Iterable[SchematicBlockPinSpec], width: float \| None = None, height: float \| None = None, lead_length: float = 6, pin_pitch: float = 10, pin_label_offset: float = 2, side_layouts: Iterable[SchematicBlockSideLayoutSpec] = (), center_label: str \| None = None, bottom_label: str \| None = None, pin_labels: bool = True, pin_numbers: bool = False, pin_number_offset: float = 2, variant: str = 'default') -> SchematicSymbolSpec` | Build a rectangular block schematic symbol from pin placement specs. |
| `ic(name: str, *, pins: Iterable[SchematicBlockPinSpec], width: float \| None = None, height: float \| None = None, lead_length: float = 6, pin_pitch: float = 10, pin_label_offset: float = 2, side_layouts: Iterable[SchematicBlockSideLayoutSpec] = (), center_label: str \| None = None, bottom_label: str \| None = None, pin_labels: bool = True, pin_numbers: bool = False, pin_number_offset: float = 2, variant: str = 'default') -> SchematicSymbolSpec` | Build an IC-style schematic symbol from pin placement specs. |
| `line(start: tuple[float, float], end: tuple[float, float], *, role: str \| None = None) -> dict` | Create a line primitive for a custom schematic symbol. |
| `terminal_lead(start: tuple[float, float], end: tuple[float, float], *, terminal: str) -> dict` | Create a terminal lead line primitive for a custom schematic symbol. |
| `rectangle(first_corner: tuple[float, float], second_corner: tuple[float, float]) -> dict` | Create a rectangle primitive for a custom schematic symbol. |
| `circle(center: tuple[float, float], radius: float) -> dict` | Create a circle primitive for a custom schematic symbol. |
| `arc(center: tuple[float, float], radius: float, start_degrees: float, sweep_degrees: float) -> dict` | Create an arc primitive for a custom schematic symbol. |
| `text(text: str, at: tuple[float, float], orientation: str = 'Right', *, align: str = 'middle', baseline: str = 'baseline', font_size: float \| None = None) -> dict` | Create a text primitive for a custom schematic symbol. |
