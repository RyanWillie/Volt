---
title: Logical
description: Generated reference for logical circuit handles and hierarchy inspection records.
---
This page is generated from the public Python source. Add or improve docstrings in `python/volt` to improve the reference.

## `volt.Component`

Handle to a kernel-owned component instance.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this component instance. |
| `reference: str` | Return the schematic reference designator for this component. |
| `schematic_symbol: SchematicSymbolSpec \| str \| None` | Return the default schematic symbol for this component, if one is registered. |

### Methods

| Method | Summary |
| --- | --- |
| `schematic_symbol_variant(self, variant: str) -> SchematicSymbolSpec \| str \| None` | Return the schematic symbol registered for a named symbol variant. |
| `__getitem__(self, key: int \| str) -> Pin` | Return a component pin by physical number, name, or explicit alias. |
| `pins(self, name: str) -> PinGroup` | Return every component pin that has the given name. |
| `select_part(self, *, manufacturer: str, part_number: str, package: str, footprint: FootprintInput, pin_pads: dict[int \| str, PinPadValue], properties: dict \| None = None, voltage_rating: float \| None = None, power_rating: float \| None = None, model_3d: PartModel3D \| None = None, approved_alternate_mpns: Iterable[str] = (), selection_override: bool = False) -> Component` | Attach selected physical part data; pass a Footprint object for board-ready geometry. |
| `dnp(self, value: bool = True) -> Component` | Set explicit do-not-populate assembly intent for this component. |

## `volt.ComponentDefinition`

Handle to a kernel-owned reusable component definition.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this component definition. |

## `volt.ModuleComponent`

Handle to a component occurrence inside a reusable module definition.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this module component template. |

### Methods

| Method | Summary |
| --- | --- |
| `__getitem__(self, key: int \| str) -> ModulePin` | Return a module component pin by physical number, name, or explicit alias. |
| `pins(self, name: str) -> ModulePinGroup` | Return every module component pin that has the given name. |

## `volt.ModuleComponentInfo`

Read-only view of a component template inside a module definition.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `index: int` |
| `definition: int` |
| `reference: str` |

## `volt.ModuleComponentOriginInfo`

Read-only view of a concrete component created from a module component template.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `module_component: int` |
| `component: int` |

## `volt.ModuleConnectionInfo`

Read-only view of a module-local pin-to-net connection.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `net: int` |
| `component: int` |
| `pin_definition: int` |

## `volt.ModuleDefinition`

Handle to a kernel-owned reusable module definition.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this module definition. |

### Methods

| Method | Summary |
| --- | --- |
| `net(self, name: str, *, kind: str = 'signal') -> ModuleNet` | Create a template-local net inside this module definition. |
| `port(self, name: str, *, kind: str = 'signal', role: str = 'passive', required: bool = True) -> ModulePort` | Create a module boundary port backed by an internal template net. |
| `instantiate(self, definition: ComponentDefinition \| LibraryComponent, *, ref: str, properties: dict \| None = None) -> ModuleComponent` | Instantiate a component template inside this module definition. |
| `connect(self, *endpoints) -> ModuleNet` | Connect module-local pins to exactly one module net or port. |
| `template_nets(self) -> tuple[TemplateNetInfo, ...]` | Return read-only descriptions of this module's template nets. |
| `ports(self) -> tuple[PortInfo, ...]` | Return read-only descriptions of this module's boundary ports. |
| `components(self) -> tuple[ModuleComponentInfo, ...]` | Return read-only descriptions of component templates in this module. |
| `connections(self) -> tuple[ModuleConnectionInfo, ...]` | Return read-only descriptions of module-local pin connections. |

## `volt.ModuleInstance`

Handle to a kernel-owned root module instance.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this root module instance. |

### Methods

| Method | Summary |
| --- | --- |
| `__getitem__(self, key: str) -> ModuleInstancePort` | Return a root module instance port by port name. |
| `component(self, ref: str) -> Component` | Return the concrete component created from a module component reference. |
| `net_origins(self) -> tuple[ModuleNetOriginInfo, ...]` | Return concrete nets mapped back to their module template nets. |
| `component_origins(self) -> tuple[ModuleComponentOriginInfo, ...]` | Return concrete components mapped back to their module templates. |
| `port_bindings(self) -> tuple[PortBindingInfo, ...]` | Return parent-net bindings for this module instance's ports. |

## `volt.ModuleInstancePort`

Handle to a root module instance port endpoint.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `port_index: int` | Return the kernel index for the module port definition. |

## `volt.ModuleNetOriginInfo`

Read-only view of a concrete net created from a module template net.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `template_net: int` |
| `net: int` |

## `volt.ModuleNet`

Handle to a template-local module net.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this module template net. |

### Methods

| Method | Summary |
| --- | --- |
| `connect(self, *pins: ModulePin \| Iterable[ModulePin]) -> ModuleNet` | Connect module-local pins to this template net. |
| `__iadd__(self, pins: ModulePin \| Iterable[ModulePin]) -> ModuleNet` | Connect module-local pins to this template net with ``+=`` syntax. |

## `volt.ModulePinGroup`

Repeated-label module component pins selected as a group.

Source: `python/volt/logical.py`

### Methods

| Method | Summary |
| --- | --- |
| `__iter__(self) -> Iterator[ModulePin]` | Iterate over the module pins in this repeated-name group. |
| `__len__(self) -> int` | Return the number of module pins in this group. |
| `__getitem__(self, index: int) -> ModulePin` | Return one module pin from this group by positional index. |

## `volt.ModulePin`

Handle to a reusable pin definition on a module-local component template.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this module-local pin definition. |

## `volt.ModulePort`

Handle to a module boundary port and its internal template net.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `port_index: int` | Return the kernel index for this module boundary port. |

## `volt.Net`

Handle to a kernel-owned logical net.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this logical net. |

### Methods

| Method | Summary |
| --- | --- |
| `mark_stub(self) -> Net` | Mark this net as intentionally exposed by a schematic stub. |
| `pins(self) -> tuple[Pin, ...]` | Return the concrete pins currently connected to this net. |
| `connect(self, *pins: Pin \| ModuleInstancePort \| Iterable[Pin \| ModuleInstancePort]) -> Net` | Connect concrete pins or module instance ports to this logical net. |
| `__iadd__(self, pins: Pin \| ModuleInstancePort \| Iterable[Pin \| ModuleInstancePort]) -> Net` | Connect pins or module instance ports to this net with ``+=`` syntax. |

## `volt.NetClass`

Handle to a kernel-owned net class and its rule provenance.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this net class. |

### Methods

| Method | Summary |
| --- | --- |
| `assign(self, *nets: Net \| Iterable[Net]) -> NetClass` | Assign this net class to one or more logical nets. |
| `info(self) -> dict` | Return the kernel-owned rule values and derivation provenance. |

## `volt.Pin`

Handle to a kernel-owned concrete pin.

Source: `python/volt/logical.py`

### Properties

| Property | Summary |
| --- | --- |
| `index: int` | Return the kernel index for this concrete pin. |
| `component: Component` | Return the component that owns this concrete pin. |
| `component_reference: str` | Return the reference designator for the component that owns this pin. |
| `name: str` | Return the logical pin name from its reusable pin definition. |
| `number: str` | Return the logical pin number from its reusable pin definition. |

### Methods

| Method | Summary |
| --- | --- |
| `mark_no_connect(self) -> Pin` | Mark this pin as intentionally unconnected for diagnostics. |

## `volt.PinGroup`

Repeated-label concrete pins selected as a group.

Source: `python/volt/logical.py`

### Methods

| Method | Summary |
| --- | --- |
| `__iter__(self) -> Iterator[Pin]` | Iterate over the concrete pins in this repeated-name group. |
| `__len__(self) -> int` | Return the number of concrete pins in this group. |
| `__getitem__(self, index: int) -> Pin` | Return one concrete pin from this group by positional index. |

## `volt.PortBindingInfo`

Read-only view of a module instance port binding edge.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `port: int` |
| `internal_net: int` |
| `parent_net: int` |

## `volt.PortInfo`

Read-only view of a module port definition.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `index: int` |
| `name: str` |
| `internal_net: int` |
| `role: str` |
| `required: bool` |

## `volt.TemplateNetInfo`

Read-only view of a module template-local net.

Source: `python/volt/logical.py`

### Fields

| Field |
| --- |
| `index: int` |
| `name: str` |
| `kind: str` |
