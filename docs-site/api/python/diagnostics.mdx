---
title: Diagnostics
description: Generated reference for diagnostics and validation report objects.
---
This page is generated from the public Python source. Add or improve docstrings in `python/volt` to improve the reference.

## `volt.Diagnostic`

Kernel-produced diagnostic exposed through the Python facade.

Source: `python/volt/diagnostics.py`

### Fields

| Field |
| --- |
| `severity: str` |
| `code: str` |
| `message: str` |
| `entities: tuple[DiagnosticEntity, ...]` |
| `category: str` |
| `overlays: tuple[DiagnosticOverlay, ...]` |
| `measurement: DiagnosticMeasurement \| None` |
| `rule: str \| None` |

## `volt.DiagnosticEntity`

Reference to a kernel entity involved in a diagnostic.

Source: `python/volt/diagnostics.py`

### Fields

| Field |
| --- |
| `kind: str` |
| `index: int` |

## `volt.DiagnosticMeasurement`

Typed actual-versus-required measurement attached to a diagnostic.

Source: `python/volt/diagnostics.py`

### Fields

| Field |
| --- |
| `actual_mm: float` |
| `required_mm: float` |

## `volt.DiagnosticOverlay`

Overlay-ready geometry and references attached to a diagnostic.

Source: `python/volt/diagnostics.py`

### Fields

| Field |
| --- |
| `kind: str` |
| `points: tuple[tuple[float, float], ...]` |
| `entities: tuple[DiagnosticEntity, ...]` |
| `layers: tuple[DiagnosticEntity, ...]` |

## `volt.DiagnosticReport`

Ordered collection of kernel diagnostics.

Source: `python/volt/diagnostics.py`

### Properties

| Property | Summary |
| --- | --- |
| `has_errors: bool` | Return whether this report contains at least one error diagnostic. |

### Methods

| Method | Summary |
| --- | --- |
| `__iter__(self) -> Iterator[Diagnostic]` | Iterate over diagnostics in report order. |
| `__len__(self) -> int` | Return the number of diagnostics in this report. |
| `__getitem__(self, index: int) -> Diagnostic` | Return one diagnostic by positional index. |

## `volt.DIAGNOSTIC_CATEGORIES`

Public value or alias exported by `volt`.

Source: `python/volt/diagnostics.py`

```python
DIAGNOSTIC_CATEGORIES = tuple(_volt.diagnostic_categories())
```

## `volt.DRC_DIAGNOSTIC_CODES`

Public value or alias exported by `volt`.

Source: `python/volt/diagnostics.py`

```python
DRC_DIAGNOSTIC_CODES = tuple(_volt.drc_diagnostic_codes())
```

## `volt.ERC_DIAGNOSTIC_CODES`

Public value or alias exported by `volt`.

Source: `python/volt/diagnostics.py`

```python
ERC_DIAGNOSTIC_CODES = tuple(_volt.erc_diagnostic_codes())
```

## `volt.PCB_FABRICATION_DIAGNOSTIC_CODES`

Public value or alias exported by `volt`.

Source: `python/volt/diagnostics.py`

```python
PCB_FABRICATION_DIAGNOSTIC_CODES = tuple(_volt.pcb_fabrication_diagnostic_codes())
```

## `volt.PCB_VISUAL_DIAGNOSTIC_CODES`

Public value or alias exported by `volt`.

Source: `python/volt/diagnostics.py`

```python
PCB_VISUAL_DIAGNOSTIC_CODES = tuple(_volt.pcb_visual_diagnostic_codes())
```
